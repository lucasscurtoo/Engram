// TODO(owner): M6 — daily local notification ("You have N cards to review") via
// UserNotifications. Ask permission gracefully; if denied, the app works without
// reminders (no errors surfaced, just no notifications). Configurable time + opt-out.
