import SwiftUI

/// TODO(owner): M6 — Swift Charts over StatsService: reviews/day (30d bars),
/// cards by state, real retention, due today / next 7 days, focus minutes/day.
struct StatsView: View {
    var body: some View {
        Text("Stats — M6")
            .foregroundStyle(.secondary)
    }
}
