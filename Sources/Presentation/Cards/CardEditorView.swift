import SwiftUI
import Domain

/// TODO(owner): M3 — note editor GENERATED from the NoteType's `fields` (never a
/// hardcoded front/back pair): one input per FieldDef with live render preview
/// (FieldContentView), tag editing, deck picker. Reused by Quick Add.
struct CardEditorView: View {
    var body: some View {
        Text("Note editor — M3")
            .foregroundStyle(.secondary)
    }
}
