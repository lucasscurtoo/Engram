import SwiftUI

/// TODO(owner): M3 — note browser: search by text and tag (NoteQuery), edit/delete.
struct CardListView: View {
    var body: some View {
        Text("Note browser — M3")
            .foregroundStyle(.secondary)
    }
}
