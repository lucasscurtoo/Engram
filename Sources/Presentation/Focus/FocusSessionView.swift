import SwiftUI

/// TODO(owner): M5 — distraction-free full-screen view: only timer + (optionally)
/// the study card during `focusing`; break screen between blocks; goal progress bar.
/// No sidebar, no visual noise.
struct FocusSessionView: View {
    var body: some View {
        Text("Focus mode — M5")
            .foregroundStyle(.secondary)
    }
}
