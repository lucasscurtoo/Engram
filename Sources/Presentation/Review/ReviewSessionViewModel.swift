import Foundation
import Application

/// TODO(owner): M4 — @Observable wrapper over ReviewSessionService (the StudySession
/// actor): current item, revealed state, preview intervals, grade actions, summary.
@Observable
final class ReviewSessionViewModel {
}
