import Foundation
import Application

/// TODO(owner): M3 — @Observable view model over DeckService.summaries.
/// Owns sidebar selection (AppRoute) and deck CRUD actions.
@Observable
final class DeckListViewModel {
}
